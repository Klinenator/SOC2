# Deploy keys — never commit
.deploy_key
.deploy_key.pub

# Runtime data — keep structure, ignore contents
uploads/*
!uploads/.gitkeep
data/evidence.json

# Secrets & reminder runtime state — never commit
data/smtp.json
data/reminder_log.json

# OS
.DS_Store
Thumbs.db
